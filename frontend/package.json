{
  "name": "chatvault",
  "private": true,
  "version": "1.18.0",
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "API_HOST=http://localhost:8080/api nuxt dev ",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "upgrade": "nuxt upgrade --force",
    "postinstall": "nuxt prepare"
  },
  "engines": {
    "node": ">=25.0.0"
  },
  "devDependencies": {
    "nuxt": "^3.17.5"
  },
  "dependencies": {
    "@pinia/nuxt": "^0.5.1",
    "bootstrap": "^5.3.2",
    "pinia": "^2.1.7"
  }
}
